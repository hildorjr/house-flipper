{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma migrate deploy && npm run db:seed && npm run build",
  "regions": ["gru1"],
  "crons": [
    {
      "path": "/api/cron/recurring",
      "schedule": "0 9 * * *"
    }
  ]
}
